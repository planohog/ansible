---
  - name: Shell Examples
    hosts: "{{ nodes }}"
    tasks:

    - name: Check Date
      shell:
         "date"
      register: datecmd
      tags: datecmd

    - debug: msg="{{ datecmd.stdout }}"
